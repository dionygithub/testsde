{#
/**
 * @file user_premium_entity.html.twig
 * Default theme implementation to present User premium entity data.
 *
 * This template is used when viewing User premium entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_user_premium_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('user_premium_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
