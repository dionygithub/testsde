{% if data['testsDestacados']|length > 0 %}

    <section class="mt-5 wow fadeIn">

            <blockquote class="blockquote bq-info mb-4">
                <h2 class="bq-title">Últimos tests del 2020!!!</h2>
            </blockquote>
        <div class="row">
            {% for index, test in data['testsDestacados'] %}

                {{ test }}

            {% endfor %}

        </div>
    </section>

    <hr class="my-5">

{% endif %}