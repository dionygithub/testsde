<div class="container-actions">

    {% if data['logged'] != false %}

        {{ data['viewuser'] }}

        <blockquote class="blockquote bq-info mb-4">
            <p class="bq-title">Cómo ganar <span class="sub-titulo" >puntos!!!</span></p>

        </blockquote>

        <div class="row col-lg-12 col-md-12 mb-4">

            {% if data['viewaction']|length > 0 %}
                {% for index, action in data['viewaction'] %}

                    <div class="col-lg-3 col-md-6">
                        {{ action }}
                    </div>
                {% endfor %}

            {% endif %}
        </div>

    {% else %}
        <h2>Debes de estar logueado en nuestra plataforma</h2>
    {% endif %}

</div>

