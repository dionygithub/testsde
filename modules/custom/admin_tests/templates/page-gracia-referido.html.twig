<div class="container-gracia-referred">

    {% if data['error'] != true %}
        {% set texto = data['texto_success'] %}
    {% else %}
        {% set texto = 'Ohh lo sentimos, pero ha ocurrido un error!!!' %}
    {% endif %}

    <blockquote class="blockquote bq-info title-gracias-referido mb-4">
        <div class="bq-title">{{ texto | raw }} </div>
    </blockquote>

</div>

{{ data['tests_destacados'] }}

